### =============================================================
### This configuration file is used by CircleCI build server
### https://circleci.com/docs/config-sample
### =============================================================

version: 2

jobs:

  # using tox
  toxify:

      docker:
        - image: python:3.6

      steps:

        - checkout

        - run: pip install tox && tox -q

workflows:
  version: 2

  myproj:
    jobs:
      - toxify