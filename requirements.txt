# NOTE: any requirements added here should also be added to setup.py REQUIRES list
certifi >= 2017.4.17
six >= 1.10
python_dateutil >= 2.5.3
setuptools == 44.1.1  # Last version with Python 2 support https://setuptools.readthedocs.io/en/latest/history.html#v45-0-0
urllib3 >= 1.23
tzlocal >= 2.0.0
# NOTE: any requirements added here should also be added to setup.py REQUIRES list